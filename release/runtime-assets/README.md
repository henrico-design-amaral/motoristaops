MotoristaOPS runtime asset bundle chunks. Each `part-XX.b64` file is an independently Base64-encoded binary slice. The landing deploy workflow decodes the slices in lexical order, reconstructs `runtime-assets.zip`, verifies its SHA-256, and extracts the versioned profile and official iconography into the public release. Dashboard files are outside this release path and are never modified by this workflow.
